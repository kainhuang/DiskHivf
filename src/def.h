#pragma once
#include <string>
#include <cstdlib>
#include <inttypes.h>
#include "common.h"

namespace disk_hivf {
    const Uint HASH_SEED = 137;
    const Float EPS = 1e-8;
    const Float INF = 1e12;
    const uint32_t MAX_UINT = (1LL << 32) - 1;
    const uint32_t SEED = 709394;
}